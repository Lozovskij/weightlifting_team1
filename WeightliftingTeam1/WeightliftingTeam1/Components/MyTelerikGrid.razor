@using WeightliftingTeam1.Data
@using WeightliftingTeam1.ModelsForOutput


@switch (PanelType)
{
    case PanelType.Attempts:
        @if (DataForGrid == null)
        {
            <p>Loading Attempts......</p>
        }
        else
        {
            <TelerikGrid Data="@(DataForGrid.Cast<Attempt>().ToList())"
                         Height="500px" PageSize="6"
                         Pageable="true" Sortable="true" Groupable="false"
                         Resizable="false" Reorderable="true" AutoGenerateColumns="true"
                         Width="100%">
                <GridToolBar>
                    <GridSearchBox />
                </GridToolBar>
                <GridColumns>
                    <GridAutoGeneratedColumns ColumnWidth="100px" />
                </GridColumns>
            </TelerikGrid>
        }
        break;
    case PanelType.Athletes:
        @if (DataForGrid == null)
        {
            <p>Loading Athletes......</p>
        }
        else
        {
            <TelerikGrid Data="@(DataForGrid.Cast<Athlete>().ToList())"
                         Height="500px" PageSize="6"
                         Pageable="true" Sortable="true" Groupable="false"
                         Resizable="false" Reorderable="true" AutoGenerateColumns="true"
                         Width="100%">
                <GridToolBar>
                    <GridSearchBox />
                </GridToolBar>
                <GridColumns>
                    <GridAutoGeneratedColumns ColumnWidth="100px" />
                </GridColumns>
            </TelerikGrid>
        }
        break;
}